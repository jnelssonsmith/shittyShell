cd - BASIC/ADVANCED

NAME:
    cd - change the current working directory

SYNOPSIS:
    cd <OPTIONAL: directory>

DESCRIPTION:
    cd will change into the directory given by <directory>, if no argument is given, it will change into the home directory of the system
